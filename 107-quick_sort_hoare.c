#include "sort.h"

/**
 * quick_sort_hoare - Sorts array using bitonic sort
 * @array: array to sort
 * @size: size of array
 */
void quick_sort_hoare(int *array, size_t size)
{
	if (!array || !size)
		return;
}
